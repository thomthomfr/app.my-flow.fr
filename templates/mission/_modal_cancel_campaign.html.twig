<div class="modal fade" tabindex="-1" id="cancelCampaignModal" {{ stimulus_target('order--index', 'cancelCampaignModal') }}>
    <div class="modal-dialog">
        <div class="modal-content">
            {{ form_start(formCancelCampaign, {'attr':{'data-order--index-target':'cancelCampaignForm'}}) }}
                <div class="modal-header custom-header">
                    <h5 class=" text-center">ANNULER LA CAMPAGNE</h5>
                    <button class="btn btn-sm btn-light custom-mr btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body text-center">
                    <p class="text-accept1 text-black">Etes-vous sur de vouloir annuler la campagne ?</p>
                    <p class="text-accept1 text-black">Toutes les missions de cette campagne seront annulées.</p>
                        {{ form_label(formCancelCampaign.cancelReason, null, {'label_attr': {'class':'form-label'}}) }}
                        {{ form_widget(formCancelCampaign.cancelReason, {'attr': {'class':'form-control form-control-solid'}}) }}
                        {{ form_errors(formCancelCampaign.cancelReason) }}
                       <div class="bouton-validate">
                           <button
                              class="btn btn-green-mission mt-5"
                           >
                                <span class="indicator-label">
                                     Je valide
                                </span>
                           </button>
                       </div>
                </div>
            {{ form_end(formCancelCampaign) }}
        </div>
    </div>
</div>
